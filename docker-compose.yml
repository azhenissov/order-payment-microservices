services:
  order_db:
    image: postgres:15-alpine
    container_name: order_db
    environment:
      POSTGRES_USER: order_user
      POSTGRES_PASSWORD: order_password
      POSTGRES_DB: orders_db
    ports:
      - "5434:5432"
    volumes:
      - order_data:/var/lib/postgresql/data
      - ./order-service/migrations:/docker-entrypoint-initdb.d

  payment_db:
    image: postgres:15-alpine
    container_name: payment_db
    environment:
      POSTGRES_USER: payment_user
      POSTGRES_PASSWORD: payment_password
      POSTGRES_DB: payments_db
    ports:
      - "5433:5432"
    volumes:
      - payment_data:/var/lib/postgresql/data
      - ./payment-service/migrations:/docker-entrypoint-initdb.d

  # MIGRATION: Payment Service теперь работает на gRPC (вместо REST)
  payment-service:
    build:
      context: ./payment-service
      dockerfile: Dockerfile
    container_name: payment-service
    depends_on:
      - payment_db
    environment:
      PAYMENT_GRPC_PORT: :9090
      DATABASE_URL: "postgres://payment_user:payment_password@payment_db:5432/payments_db?sslmode=disable"
    ports:
      - "9090:9090"  # gRPC Server (вместо 8081 REST)
    volumes:
      - ./payment-service:/app

  # MIGRATION: Order Service теперь работает с gRPC клиентом (вместо HTTP)
  order-service:
    build:
      context: ./order-service
      dockerfile: Dockerfile
    container_name: order-service
    depends_on:
      - order_db
      - payment-service
    environment:
      ORDER_REST_PORT: :8080
      ORDER_GRPC_PORT: :9091
      PAYMENT_GRPC_ADDRESS: payment-service:9090  # gRPC address (вместо HTTP)
      DATABASE_URL: "postgres://order_user:order_password@order_db:5432/orders_db?sslmode=disable"
    ports:
      - "8080:8080"  # REST API (для пользователей)
      - "9091:9091"  # gRPC Server (для streaming)
    volumes:
      - ./order-service:/app

volumes:
  order_data:
  payment_data:
